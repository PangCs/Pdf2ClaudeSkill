<Window x:Class="PdfExtractToSkill.Views.UninstallCleanupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prepare to Uninstall" Width="480" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <StackPanel Margin="16">

        <TextBlock FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"
                   Text="Prepare PDF Extract to Skill for uninstall"/>

        <TextBlock TextWrapping="Wrap" Margin="0,0,0,12">
            This will remove the Windows startup entry so the app no longer launches automatically.
            Your extracted skill files and installed Claude Code skills are never deleted.
        </TextBlock>

        <CheckBox IsChecked="{Binding DeleteConfig}" Margin="0,0,0,16">
            <TextBlock Text="{Binding ConfigFolderPath, StringFormat='Also delete config folder: \{0\}'}"
                       TextWrapping="Wrap"/>
        </CheckBox>

        <TextBlock TextWrapping="Wrap" Foreground="Gray" FontSize="11" Margin="0,0,0,16">
            After clicking Clean Up, go to Settings → Apps → Installed apps to complete the uninstall.
        </TextBlock>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}"
                    Width="80" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Clean Up" Command="{Binding CleanUpCommand}"
                    Width="90" IsDefault="True"/>
        </StackPanel>

    </StackPanel>
</Window>
